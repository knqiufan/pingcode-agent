- 检查 .env 配置文件中的 pingcode 配置是否还有用，无用则删除
- 元数据管理中都不要显示出项目 id 列，应该都要与项目表进行关联后展示出项目名称。
- 数据同步模块，目标项目支持添加识别到的项目，即若用户选择的项目在pingcode中没有，支持调用创建项目pingcode接口先创建项目，再在该项目下创建工作项目。
- agent 需求识别添加支持：支持识别需求文档中每一个工作项的负责人（若没有则默认当前用户）；支持识别出每一个工作项之后都会给出解决方案建议（开发建议、bug修复建议、解决步骤等，根据工作项的类型自动识别）
- 工作项目列表中的所有项目都支持编辑和详情查看。在进行相似度匹配时，若匹配度低于0.6，匹配状态可表示为没有相似项目
- 在需求分析解析完成之后，支持手动添加需求与工作项，避免 agent 解析有误。
- 在进行数据同步入库时，需要将获取到的数据去重后再入库。
- 不要把模型配置写在配置文件中，根据当前项目架构，设计规划编写一个模型管理模块，支持OpenAI和Anthropic规范自定义接口，自定义模型配置，让用户自行填写选择模型。
- 点击导入pingcode按钮后，解析出来的工作项目和项目都需要有记录。根据当前项目结构，设计规划编写解析记录留痕功能，对于使用该系统导入的需求都需要有记录留痕。
- 根据当前项目架构，设计规划编写用户管理模块。需求记录留痕中需要与用户相关联，即登录用户只能查看自己导入的需求记录。同时注册功能也要进行相应的重构和修改。
- 根据当前项目架构，设计规划编写角色管理模块。支持权限控制，包括功能权限和数据权限。
- 所有的表数据都需要与用户相关联，即当前用户登录后只能看到自己的数据，只有 admin 超级管理员才能看到所有的数据
